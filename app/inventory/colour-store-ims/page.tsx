'use client'
// Colour Store IMS — Inventory Management section. Scaffold only for now;
// content and workflow to be defined next.

export default function ColourStoreIMSPage() {
  return (
    <div className="content" style={{ padding: '16px 20px' }}>
      <div style={{ fontSize: 15, fontWeight: 700, marginBottom: 8 }}>Colour Store IMS</div>
      <div style={{ background: 'var(--bg-primary)', border: '1px solid var(--border-light)',
        borderRadius: 10, padding: 48, textAlign: 'center', color: 'var(--text-tertiary)', fontSize: 14 }}>
        This page is set up and ready — waiting on requirements for what it should contain.
      </div>
    </div>
  )
}
